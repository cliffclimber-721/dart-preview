void main(List<String> args) {
    var giveMeFive = true;
    var nums = [
      1,
      2,
      3,
      4,
      // collection if 
      // condition 'if' is true, return 5
      // same as in the list code
      if(giveMeFive) 5,
    ];
    print(nums);
}